# 接口契约清单（与旧 APK 兼容）

> 状态：`待填` — 以说明书为准，Jadx/抓包交叉验证后锁定。  
> data 层 DTO **字段名必须与下表一致**，禁止擅自驼峰化后端字段（可在 domain 层再映射）。

## 全局约定（待确认）

| 项 | 值 | 来源 |
|----|-----|------|
| BaseUrl 示例 | `http://{host}:{port}/` 或 ngrok | 说明书 / 登录配置 |
| 数据格式 | JSON / form-urlencoded / 混合 | TBD |
| 成功码 | 例如 `code == 0` 或 `200` | TBD |
| 鉴权 | Token Header / Cookie | TBD |
| 字符集 | UTF-8 | 默认 |
| ngrok Header | `ngrok-skip-browser-warning: true` | 新增强制（代理模式） |

## 响应信封（待确认）

```json
{
  "code": 0,
  "msg": "ok",
  "data": {}
}
```

（若旧版结构不同，以旧版为准替换本节。）

## 接口表

| ID | 模块 | Method | Path | 请求要点 | 响应要点 | 旧调用位置 | 验证 |
|----|------|--------|------|----------|----------|------------|------|
| A01 | 登录 | POST | TBD | user, pwd | token, userInfo | TBD | 待 |
| A02 | 健康检查/连接测试 | GET/POST | TBD | — | ok | 新增可选 | 待 |
| B01 | … | | | | | | 待 |

## 变更纪律

- 新增后端字段：可忽略解析，不得依赖。  
- 缺失旧字段：必须兼容，不得省略提交。  
- 仅 UI 需要的聚合：在 domain/ui 组装，不改协议。
